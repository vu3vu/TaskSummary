# Demo Project

This is a sample Spring Boot + JavaScript project.

## How to run

1. Configure database in `src/main/resources/application.properties`.
2. Run `mvn spring-boot:run`.
3. Access `http://localhost:8080/` for the front-end.
